# 任务：全量财务舞弊标注（第2组数据）

你将继续担任会计学AI助手。我将上传一个 Excel 文件，其中包含**我负责的全部原始违规数据**（第2组，未标注）。请你严格按照《作业说明》中的规则，为**每一行**的 Activity 字段标注以下六个字段。

## 输入文件
`STK_Violation_Main_第2组.xlsx`  
包含列：`Activity`（以及可能有的其他原始列，你只需关注 Activity）

## 输出要求
- 输出 **xlsx文件**，名称为‘STK_labeled_夏思远_2023110537_G02.xlsx’。本次任务输出的文件统一放在‘D:\5th My Projects\FOR TEST\全量数据标注’路径下。
- 必须包含以下 7 列（顺序固定）：
  1. `Activity`（原样复制）
  2. `ann_related`（0 或 1）
  3. `ann_year`（JSON 数组字符串，如 "[2017,2018]"；否则 `null`）
  4. `ann_fin_flag`（0 或 1 或 `null`）
  5. `ann_fin_info`（JSON 数组字符串，如 '[{"year":2017,"elements":["收入","资产"]}]'；否则 `null`）
  6. `third_party_flag`（0 或 1 或 `null`）
  7. `third_party_list`（JSON 数组字符串，如 '[{"name":"X公司","type":"供应商","role":"虚假合同"}]'；否则 `null`）

- **null 统一用小写字符串 `null`**。
- 行数必须与输入文件行数一致，顺序一致。

## 标注规则（完全沿用此前 Pilot 阶段的定义）
- **任务一**：年报相关性（ann_related / ann_year）  
  - 只有明确提及“××年年度报告”存在虚假/遗漏才算 1。季报/半年报不算。  
- **任务二**：财务信息与会计要素（ann_fin_flag / ann_fin_info）  
  - 前提：ann_related = 1。  
  - ann_fin_flag=1 仅当影响财务报表金额；否则 0。  
  - 会计要素限：`资产、负债、所有者权益、收入、费用、利润`。  
  - 年份与要素必须一一对应。  
- **任务三**：第三方配合造假（third_party_flag / third_party_list）  
  - 前提：ann_related=1 且 ann_fin_flag=1。  
  - 第三方不包括上市公司、子公司、实控人。  
  - 配合行为：虚假合同、资金回流、出借账户等主动协助。正常交易不算。  

##注意：这次是全量数据，可能行数较多。你可以自主决定如何高效完成。