# 任务：直接生成两个已标注的 Excel 文件（基准版 + 空白版）

我将上传两个文件：
1. `作业说明_update.html`（完整作业规范）
2. `STK_Violation_Main_第2组.xlsx`（我的原始数据，第 2 组）

请你扮演**智能财务前沿课程的助教**，严格按照 `作业说明_update.html` 中的定义，完成以下工作，并**直接输出两个 Excel 文件**，以便我下载后使用。

---

## 一、抽样要求

- 从 `STK_Violation_Main_第2组.xlsx` 中，使用 `random_state = 42` 随机抽取 **5% 的样本**（`df.sample(frac=0.05, random_state=42)`）。
- 保持原始行索引，只对抽取出的行进行后续操作。

## 二、你需要标注的字段（共6个）

严格按照 `作业说明_update.html` 中的 **“输出字段规范”** 填写：

| 字段 | 类型 | 规则 |
|------|------|------|
| `ann_related` | int (0/1) | 1 = 影响年报；0 = 不影响 |
| `ann_year` | list 或 null | 若 `ann_related=1`，填受影响年份列表，否则 null |
| `ann_fin_flag` | int 或 null | 仅在 `ann_related=1` 时判断：1=影响财务信息，0=仅非财务 |
| `ann_fin_info` | JSON list 或 null | 若 `ann_fin_flag=1`，格式 `[{"year": YYYY, "elements": ["要素1",...]}]`，要素限：资产、负债、所有者权益、收入、费用、利润 |
| `third_party_flag` | int 或 null | 仅在 `ann_related=1` 且 `ann_fin_flag=1` 时判断：1=有第三方配合，0=无 |
| `third_party_list` | JSON list 或 null | 若 `third_party_flag=1`，格式 `[{"name": "...", "type": "...", "role": "..."}]`，type 参考：客户、供应商、银行/金融机构、券商、会计师、评估机构、自然人、其他企业 |

> 注意：  
> - 季报/半年报违规 ≠ 年报违规  
> - 正常业务往来 ≠ 第三方配合  
> - 年份与要素必须一一对应，不可合并  
> - 不满足前提条件的字段一律填 `null`

## 三、你需要输出的两个 Excel 文件

### 文件1：基准答案版（作为人工标注基准）
- 文件名：`benchmark_G02.xlsx`（组号 02，姓名学号我自己后加）
- 内容：抽样数据的全部原始列 + 上述 6 个新列（已填好你的标注值）
- 格式：`ann_year` 存储为字符串列表（如 `"[2017,2018]"`），`ann_fin_info` 和 `third_party_list` 存储为 JSON 字符串

### 文件2：空白版（供其他 AI 模型填写）
- 文件名：`blank_G02.xlsx`
- 内容：与文件1相同的行和列，但 **6 个新列全部留空**（仅列名存在，单元格内容为空字符串）


## 四、你的标注依据

- 以 `作业说明_update.html` 中的判断标准、示例为**唯一依据**。  
- 必须逐条阅读 `Activity` 文本，做出明确判断，不能跳过。  
- 若文本信息不足，优先判断为 `null`（仅限前提条件不满足的情况），但尽量基于已有信息推断。

---

请现在开始执行：读取我上传的文件 → 抽样 5% → 逐条人工标注 → 生成两个 Excel 。