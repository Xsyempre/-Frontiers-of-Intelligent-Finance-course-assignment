# 子任务一：全量财务舞弊标注 – 年报相关性判断

你将继续担任会计学AI助手。我将上传一个 Excel 文件，其中包含**我负责的全部原始违规数据**（第2组，未标注）。请你严格按照《作业说明》中的规则，为**每一行**的 Activity 字段标注**前两个字段**：`ann_related` 和 `ann_year`。

## 输入文件
`STK_Violation_Main_第2组.xlsx`  
路径：`D:\5th My Projects\FOR TEST\全量数据标注\STK_Violation_Main_第2组.xlsx`  
包含列：`Activity`（以及其他原始列，你只需关注 Activity）

## 输出要求
- 输出一个 **xlsx 文件**，名称为：`STK_labeled_夏思远_2023110537_G02.xlsx`
- 保存路径：`D:\5th My Projects\FOR TEST\全量数据标注\`
- 文件中必须包含以下列（顺序固定）：
  1. `Activity`（原样复制）
  2. `ann_related`（0 或 1）
  3. `ann_year`（JSON 数组字符串，如 "[2017,2018]"；否则 `null`）

- **null 统一用小写字符串 `null`**
- 行数必须与输入文件行数一致，顺序一致

## 判断规则（严格按作业说明，仅任务一）

请仔细回顾 `作业说明_update.html` 中**任务一**的内容：

- **ann_related = 1**：只有文本明确提及“××年年度报告”（或“年报”）存在虚假记载、重大遗漏或误导性陈述。
- **ann_related = 0**：以下情形均不属于年报相关：
  - 仅提及季报、半年报
  - 仅涉及资金占用、违规担保（未提及年报数据错误）
  - 内幕交易、市场操纵
  - 仅涉及临时公告、关联交易未及时披露（未提及年报）
- 若 **ann_related = 1**，提取受影响的年份列表（如 [2017, 2018]）；若无明确年份则填 `null`。

## 关键约束
- `ann_related = 0` 时，`ann_year` 必须为 `null`

## 工作流程
1. 读取指定路径的 `STK_Violation_Main_第2组.xlsx`
2. 对每一行的 `Activity` 独立判断，输出 `ann_related` 和 `ann_year`
3. 将原始数据 + 这两个新列写入输出文件（覆盖或新建）

## 注意
- 本次是全量数据，行数可能较多。请高效处理。
- 完成后只输出该 Excel 文件，不需要额外解释。

现在请开始执行。